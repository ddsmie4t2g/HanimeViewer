# HanimeViewer 项目要点

> fork 自 `daisukiKaffuChino/Han1meViewer`，仓库 `ddsmie4t2g/HanimeViewer`，**活动分支 `mod`**。
> 最后更新：2026-09-15（v26.9.0 发布后）｜本文件**纳入版本管理**，随版本更新。

---

## 一、版本 / 发布

| 项 | 说明 |
|---|---|
| 版本号 | 纯三段（如 `26.9.0`），**不带 `-mod`**；tag 命名 `v<版本名>` |
| versionCode | `major*1_000_000 + minor*1_000 + patch`（26.9.0 → `26009000`） |
| 改版本动两处 | `app/build.gradle.kts` + 根 `update.json` |
| APK 命名 | `Han1meViewer-v<版本名>.apk` |
| 发布方式 | ⭐ **推 tag 即全自动**：`release.yml` 监听 `v[0-9]*`，CI 自编译+签名+发 Release（约 6 分钟）。手动 `gh release create` 的 notes 会被 CI 覆盖 |

- `AppUpdateChecker` 读 `BuildConfig.UPSTREAM_BASE_VERSION`（上游基线 `26.3.2`），**别从 `VERSION_NAME` 解析**。
- keystore：`~/.android/keystore.jks`，alias `hanime`；CI Secrets `RELEASE_KEYSTORE_BASE64/_PASSWORD/_KEY_ALIAS`。
- `cpp/chino.h` 的 `EXPECTED_SIG_HASH` 硬绑签名证书 → **换 keystore 必须同步改**，否则「装得上但播不了」。
- 新字符串三处都加：`values/`、`values-zh-rCN/`、`values-zh-rTW/`。
- CI 冷构建曾因 daemon 堆累积 OOM（zipflinger）→ `org.gradle.jvmargs` 保 `-Xmx4096m`；**CI 的 Build release APK 失败先怀疑 OOM**。
- ⚠️⚠️ **CI 的 `Set up Android SDK` 会随 runner 镜像滚动而挂**（表现：**40 秒就 failure、卡在 SDK 步骤、
  连编译都没开始** ⇒ 「tag 推上去了、Release 一直不出来」）。**两个互相独立的成因**：
  ① cmdline-tools 升到 **16.0** 后 `sdkmanager --licenses` 变成**两级提示**，而 action 只往 stdin 塞 10 个 `y`；
  ② action 默认 `packages: tools platform-tools` 里的 **`tools` 包已从 Google 仓库下架**。
  **修法**：`accept-android-sdk-licenses: false` + **`packages: ''`（⚠️ 这行不能删，删掉＝恢复默认值）**
  + 自己 `yes | sdkmanager --licenses` 与 `yes | sdkmanager --install ...`。**`ci.yml` / `pr_check.yml` 同病，一起改。**
  **补发别 force push tag**，用 `gh workflow run release.yml --ref mod` —— 注意 `push` 事件的重跑用的是
  **tag 所在提交**里的工作流文件，修了也白修。（完整诊断见技能 `android-apk-release` 坑 24。）
- ⚠️ 加枚举值后：**表达式** `when` 编译期报缺分支，**语句式** `when` 不报 → 用 `grep -rn "isNjavSite\|isAvSite\|when (.*siteSource"` 兜底排查。

## 二、四条铁律

1. **正则禁裸 `{` `}`**：ICU4C 只认量词（`\d{4}` 安全）。裸 `{` 写在 `object` 字段初始化 → `<clinit>` 失败 → 运行期 `NoClassDefFoundError`（只报一个类名先怀疑类初始化）。
2. **周期性刷新不得覆盖事件型状态**：`publishState()` 每 250ms 且末尾清 `errorMessage` → 错误自抹；错误/一次性提示另存常驻字段。
3. **自建 `OkHttpClient` 必挂 `HProxySelector()` + `HProxyAuthenticator.http`**（HDns DoH 例外）。
4. **更新包续传必须认身份，长度不算身份**：`update.apk.meta` 身份旁注 + 206 校验 `Content-Range` 起点 + 下完 `getPackageArchiveInfo` 自检，否则拼出「长度对、PK 头对、装不上」的包。

## 三、网络环境（国内直连三层封锁）

1. **系统 DNS 投毒** → 必须 DoH。预设 alidns 自己也返回投毒 IP，只有 `doh.pub`（120.53.53.53）拿到真 Cloudflare IP。
2. **`hanime1.me` / `hanimeone.me` 按 SNI 阻断**（同 IP：SNI 写 `.com`→200、`.me`→TLS RST）→ **默认镜像必须 `hanime1.com`**。
3. **`vdownload.hembed.com` IP 级封 443**。

⭐ **能浏览 ≠ 能播放。**

⭐⭐⭐ **普通代理（SOCKS5/HTTP）对被 SNI 阻断的站根本无效**：SNI 在 ClientHello 明文里，客户端→代理那段仍在墙内。**代理在 TLS 之下，审查在 TLS 之内。唯一解 = TLS 终结式中转**（换掉「客户端看到的对端」），上游无任何中转实现。

- ⭐⭐ **内置 IP 必须无条件生效**（26.8 修）：以前只在默认**关闭**的 `useBuiltInHosts` 打开时才用 ⇒ 大多数人一直走被投毒的系统 DNS。实测（中国移动）系统 DNS 给出的 `hanime1.com` / `hanime1.me` IP 都不通，内置表 8 个 IP **7/8 直接 200**。现在开关只管「用户自填 IP」，系统解析只当尾部兜底。
- ⭐⭐ **检查更新要「并发问所有源、取 versionCode 最大的一份」**（26.8）：以前是「第一个成功就返回」，而 jsDelivr 边缘会缓存旧内容 ⇒ **刚发的新版检测不到**。更新源 5 条：`cdn/fastly/gcore/testingcf.jsdelivr.net` + `raw.githubusercontent.com`。

### CDN 中转机制（8.0 起核心）

- 组件：`logic/network/CdnRelay.kt`（URL 重写 + 复合信任链）、`interceptor/CdnRelayInterceptor.kt`（直连优先、失败才中转）。
- **挂载点三处都要挂，少一处就有一半功能是坏的**：`NjavPlaybackInterceptor` 的 `PlaybackHttpClient.client`（播放）、`ServiceCreator.buildDownloadClient()`（下载；`hlsClient` 派生自它所以放前面）、`ImageNetworkClient`（封面图）。三处都加 `sslSocketFactory(CdnRelay.sslContext.socketFactory, CdnRelay.trustManager)` + `addInterceptor(CdnRelayInterceptor())`。
- 端点 `https://156.238.224.214:7443/r/<SECRET>/<base64url(原URL)>`，**单段路径**（`/` 会破坏分段）。白名单 `hembed.com`/`fourhoi.com`/`pornhub.com`/`phncdn.com`，**不是开放代理**。开关 `SettingsRepository.allowCdnRelay`。
- ⭐ **自建账号服务**（26.7.0）：同机 **7444**（`/opt/haccount/account.py`，SQLite，systemd `haccount`），**复用中转那张自签证书**，存「关注 / 本机清单 / 观看记录」。数据是**不透明 JSON 文档** + `revision` 乐观并发（冲突回 409），**合并规则全在客户端**（`logic/account/AccountSync.kt`，只增不删、进度取大、可交换）。键位 `SettingsRepository.accountJson`。与 hanime 站点账号完全无关。
- ⭐ **换机 = 发版**（2026-09-13 从 `186.241.94.98` 换到 `156.238.224.214`）：中转 IP 与内置证书都编译进 APK，必须成对改 `CdnRelay.HOST` + `res/raw/relay_cert.pem`（新证书 SAN 要写新 IP），改完按 skill `sni-relay-bypass` 的三段验证逐条跑。旧机别急着关（老版本客户端还连着它）。
- **服务端 `relay.py`（RELAY_PY_VER=4）两件关键事**：
  1. 反盗链 Referer 由服务端补：中转 URL 的 `?ref=` 携带，**必须在拷贝客户端头之后应用**（客户端自带 Referer 会覆盖它）。
  2. **`.m3u8` 清单必须由服务端改写成绝对地址**：Pornhub 清单里相对路径会被播放器相对解析进中转单段路径 → 中转当 base64 解 → 400。判定法：取一次清单看非注释行是否以 `http` 开头（`build/ph-manifest-check.py`）。
- 证书 `res/raw/relay_cert.pem` 打进 APK；解析前 `.replace("\r","")`（autocrlf 坑），失败降级不崩。
- 判「被墙」只看 **IOException**，**4xx 不算**（403 = token 签名不对）。死域名用会话级 Set，别持久化。
- ⚠️ 图片反盗链比视频严：对 phncdn 一律附 ref。
- ⚠️ 测字节数别用 `curl -o /dev/null`（Git Bash 下恒 0 假阴性），写真文件再 `wc -c`。

## 四、播放

- 请求头逐引擎手传（Exo/系统/mpv）；mpv 的 `http-header-fields` 必须在 `loadfile` **前** set。
- 一律 `OkHttpDataSource`（`callTimeout`=0）：`DefaultHttpDataSource` 走 `HttpURLConnection` 绕开应用网络栈 → 「详情页能开、一播放 0:00/0」。
- 播放/下载头统一走 `logic/PlaybackHeaders.forUrl(url)`：**按 host 分流**，新增站点只改这里。
- nJAV 403 头号原因 = **HTTP 版本不是防盗链**：`downloadClient` 为 hanime 直链钉死 `HTTP_1_1`，surrit 按 HTTP 层指纹拦 1.1 → 「能播不能下」先 diff 两条链路 client。
- HLS 下载（surrit `.m3u8`，分片实为 MPEG-TS）→ `downloadHls()` 逐片追加，不需要 ffmpeg；续传靠 `.hlsidx`。

## 五、数据源（三家）

`SiteSource` = `Hanime1` / `Njav` / `Pornhub`；判据 `isNjavSite` / `isPornhubSite` / `isAvSite`。
镜像族：`hanime1.com`/`.me`/`hanimeone.me` + `njavtv.com` + `pornhub.com`。

- ⭐ 选新数据源先问「有没有 JSON 接口」：Pornhub 列表 JSON ~140 KB/页 vs 列表页 HTML 1.2–1.6 MB。
- 切换数据源**必须同时写 `siteSource` 与 `domainName`**；抽屉「切换站点」为三站选择弹窗。
- 下载「先落库再联网」：`createDownloadRecord` 不依赖网络。
- ⚠️ `HANIME_BASE_URL` 会跟着当前数据源变 ⇒ 一切**站点专属**功能必须用 `SettingsRepository.hanimeBaseUrl`。
- ⭐ **「存到哪」判据 = 这条内容属于哪个站点**：`useLocalVideoState = !isAlreadyLogin || siteSource.isAvSite`；`displayVideoFlow`/`toggleFavorite`/`rateVideo`/`updateMyListSelection` 必须同判据。
- ⭐ **本地清单靠 `kind` 分流**（26.9.0）：`LocalListEntity.kind` = `playlist` / `favorite_collection`，DAO 一律 `WHERE l.kind = :kind`。导出/导入必须带 `kind`；`ListsExport.favoriteCollections` 的**默认值不能删**（否则覆盖式恢复会丢收藏夹）。
- ⭐ **关注新作红点**（26.9.0）：`FollowedArtistStore.Item.newCount/seenCodes` 驱动；`ArtistUpdateChecker` 并发 4 + 30min 节流，**首见作者只补种不报数**；后台走 WorkManager 12h 周期（`KEEP` 幂等）。服务端订阅无身份键 → 用**小写名字**兜底匹配。

### Pornhub 专项（26.6 起第三个数据源，26.6.2 大修）

- **全程走自建中转**（SNI 阻断，无直连模式，`CdnRelay.ALWAYS_RELAY_HOSTS`）。
- ⭐⭐⭐ **播放地址两种签名形态，站点随机发**：
  - A：`?validfrom=…&validto=…&hash=` → 200 可取；
  - B：`?h=…&e=…&f=1` → **必 410**（`e=` 在未来也 410，UA/Referer/Cookie 十几种组合全试过）。
  - **判据只认 `url.contains("validfrom=")`**，不认主机名（主机每次请求都在变）。
- **修法 = 双源**（26.6.2）：详情页地址可取（A 形态）直接用（240/480/720/1080 全画质）；不可取自动退 `/embed/<viewkey>`（实测 10/10 给 A 形态，仅 480P，兜底不是主路）。见 `NetworkRepo.phVideoFlow` + `PhParser.mediaLooksPlayable`。
- embed 页里**没有 `flashvars`**：`mediaDefinitions` 裸在内联 JSON 里 → 解析两条路：先 `var flashvars_<id>` 括号配对，再退回找 `"mediaDefinitions"` 键名取 `[]`（`quality` 字段类型在 String/Array 间变）。
- 列表走 JSON 接口 `/webmasters/search`（`ordering` 只有 `newest`/`mostviewed`/`rating` 真生效；`tags[]` 用无效 slug 返回空列表）。
- ⭐⭐⭐ **详情页按 UA 给两套 DOM**：app 默认移动 UA 下 `video-detailed-info` 等整块不存在 → Pornhub client 强塞 `DesktopUserAgentInterceptor`（用 `.header()` 替换，挂在其后）。
- 作者：**素人片作者在「主模特」区块（`userInfoBlock`），有挂牌演员的在 `pornstarsWrapper`（`a.pstar-list-btn`）**——两块都读、按名字去重、主模特排最前，只读后者的会漏素人片（26.6.1「显示不全」根因）。⚠️ `usernameWrap` 全页 107 个（推荐位卡片），只能在 `video-detailed-info` 内取。
- 作者关注：Pornhub/nJAV 无订阅接口 → **本地关注** `logic/FollowedArtistStore`（DataStore JSON，身份键优先主页 url），UI 按钮 + 合并进「我的关注」页；hanime 走原服务端订阅，`VideoRouteActions.toggleArtistSubscription` 按 `artist.post == null` 分流。
- 作者主页只给 6–9 条、`?page=2` 404 → 完整列表只能靠按名字检索。

## 六、当前状态（2026-09-15）

### 已发布版本

| 版本 | 主要改动 |
|---|---|
| 26.6.1 | 首页栏目 4→10、作者全量展示、切换站点弹窗、域名补 Pornhub、桌面 UA 修详情页 |
| 26.6.2 | **修 410**（双源）、**作者显示补全**（主模特+演员合并）、**新增本地关注按钮** |
| 26.6.3 | 作者页（首屏资料头）、关注免登录、中转换机 |
| 26.6.4 | 作者页首屏立刻显示资料头 + 失败可重试 |
| 26.6.5 | 修跨站关注 404；hanime 也有作者页 |
| 26.7.0 | ⭐ **自建账号** —— 数据存在你自己的服务器上 |
| 26.7.1 | 账号入口真的摆出来；分清「我的账号」与「hanime 站点账号」 |
| 26.7.2 | 修「注册失败但看不到原因」：中文用户名可用 + 错误本地化 |
| 26.7.3 | 修「登录不上 hanime」；收藏 / 订阅按站点归属落地 |
| 26.8 | ⭐ **nJAV 大修**；登录 / 检查更新改走内置 IP |
| 26.8.1 | nJAV 分类补齐 —— 素人 / 无码 / 亚洲三组系列 |
| 26.8.2 | 女优一览 / 女优排行 + 女优头像提速 |
| 26.8.3 | 日历回到只认里番 + 作者页头像加厚兜底 |
| **26.9.2**（当前） | 修「检测更新很慢」：**更新源赛跑**（首个应答先生效、到点掐掉挂死的源）+ 修钉错的 jsDelivr IP（gcore/testingcf 曾钉黑洞的 Cloudflare 段）+ **首页不再等更新检查**（实测 30 s → 1.8 s） |
| 26.9.1 | 修「作者 / 女优的作品列表最多只显示十页」——分页死锁（`canNext` 挂在总页数上 + 作品数解析恒 null）+ 跳页只拉两批；`ArtistVideosPage` 加「站点总页数」（hanime 作者页现在能看到一共多少页） |
| 26.9.0 | **删 getchu 全链路与「一键诊断/自愈」**；**封面「假死」真因修复**（重试改换 Coil 缓存 key）；**独立收藏夹**；**关注作者新作红点**；更新说明 markdown 渲染；作者页 2 列×6 行 + 编号分页；在线历史按登录态分文案 |

### 26.9.0 验证

- 全量单测 **59 条 0 失败**；`:app:compileReleaseKotlin` 通过；CI 自检 `chino.h` 签名指纹一致 ✓。
- 删除的类全部无残留引用（`Getchu*` / `NetworkDiagnostics` / `NetworkSelfHeal` / `SelfHealDialog` / `ic_heal.xml`）。
- 中转节点池未受影响（`RelayNodeStore` / `RelayNodeDialog` / 「网络设置」入口保留）。
- ⭐ 教训：**随机形态的问题必须抓多份快照**，只抓一份会把随机看成常态。

### 26.9.1 验证

- 全量单测 **77 条 0 失败**（新增 18 条：`ArtistPagingTest` 13 + `ParserHanimeTotalPagesTest` 5）；
  `:app:compileReleaseKotlin` 与 `:app:assembleRelease` 通过，APK 证书 SHA-256 与 `chino.h` 一致。
- 分页数学与真实快照对照：`build/pager_sim.py`（旧：Pornhub 40 条/页**卡在第 10 页** → 新：第 100 页；
  hanime 59 条/页 × 20 页：旧卡第 59 页 → 新第 99 页）。
- 站点总页数选择器离线校验：`build/VerifyHanimeTotal2.java`（s1..s20 / g1..g13 → 20 / 13，含**末页**）。

### 26.9.2 验证

- 全量单测 **84 条 0 失败**（新增 `AppUpdateSourceRaceTest` 7 条：挂死的源不拖时间 / 取最大 /
  没更新等满预算 / 全部挂死只在首个预算后放弃）；`:app:compileReleaseKotlin` 通过。
- ⭐ **检查更新实测**（`build/probe_update_speed.py`，真实更新地址，两套策略对照）：
  旧「等全部 + connect 15 s + 旧 IP 表」**30.0 s** → 新「源赛跑 + connect 5 s + 新 IP 表」**1.8 s**；
  只修 IP 表 2.3 s、只修等待策略 4.0 s —— 两个成因各占一半。
- 钉 IP 实测（2026-09-15，中国大陆直连）：四个 jsDelivr 域名在 `151.101.x`（Fastly）全部 200，
  在 `104.17.x`（Cloudflare）全部 connect 不返回。

### 已知问题

1. **收藏夹与播放清单仍共用一张表**，只是 `kind` 分流。若以后要支持「一个片子同时进多个清单」，需拆成关联表 —— 当前不需要。
2. 站点作者主页本身作品不全（6–9 条 + 分页 404）→ 完整列表只能靠按名字检索；App 侧已用编号分页呈现，但补不了站点缺的数据。
3. jsDelivr 边缘缓存最长 12h 自愈，`update.json` 发版后已 purge 但传播仍慢（本机无法手动 purge）。

### 测试 / 校验脚本（`build/` 下，可复用，已 gitignore）

- `VerifyPh.java` — PhParser 离线校验器（双源 + 作者合并），jsoup + org.json。
- `ph-manifest-check.py` — 中转清单绝对地址 + 分片 ref 校验。
- `ph-embed-check.py` / `ph-410-check.py` / `ph-form-check.py` / `ph-cookie-check.py` — 形态与链路排查。
- `set_update.py` — 写 `update.json`。
- `commit-<版本>.txt` / `desc-<版本>.md` — 提交说明与更新公告留档。

## 七、构建命令

```bash
export JAVA_HOME="C:/Android/jdk-21.0.12+8"
export ANDROID_HOME="C:/Android/sdk"
export ANDROID_SDK_ROOT="C:/Android/sdk"
./gradlew :app:compileReleaseKotlin --console=plain   # 编译验证
./gradlew :app:assembleRelease --console=plain        # 打包（本地仅当编译验证，发布走 CI）
./gradlew :app:testDebugUnitTest --console=plain      # 全量单测
```

> 必须用工程自带的 `./gradlew`：AGP 9.2.1 要求 Gradle ≥ 9.4.1，系统的 8.14.3 会报版本不够。
